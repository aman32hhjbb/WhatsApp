<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Whatshap" parent="Theme.Material3.Light.NoActionBar">
        <item name="android:textColor">@color/black</item>
        <item name="android:statusBarColor">@color/main_body</item>
        <item name="colorPrimary">@color/main_body</item>
        <item name="android:fontFamily">@font/late_regular</item>
    </style>
    <style name="Base.Theme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:textColor">@color/black</item>
        <item name="android:statusBarColor">@color/main_body</item>
        <item name="colorPrimary">@color/main_body</item>
        <item name="android:fontFamily">@font/late_regular</item>
    </style>
</resources>